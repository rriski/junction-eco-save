# Migration `20201107000951-add_building_data`

This migration has been generated by Henrik Aarnio at 11/7/2020, 2:09:51 AM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
ALTER TABLE "public"."Building" ADD COLUMN "construction_date" timestamp(3)   NOT NULL ,
ADD COLUMN "use_category" text   NOT NULL ,
ADD COLUMN "heating_category" text   NOT NULL ,
ADD COLUMN "fuel_category" text   NOT NULL ,
ADD COLUMN "construction_material" text   NOT NULL ,
ADD COLUMN "area_living" Decimal(65,30)   NOT NULL ,
ADD COLUMN "area_floors" Decimal(65,30)   NOT NULL ,
ADD COLUMN "floors" integer   NOT NULL ,
ADD COLUMN "location_street_address" text   NOT NULL ,
ADD COLUMN "location_street_number" integer   NOT NULL ,
ADD COLUMN "location_post_number" text   NOT NULL 
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20201106220834-create_building_renovation..20201107000951-add_building_data
--- datamodel.dml
+++ datamodel.dml
@@ -2,9 +2,9 @@
 // learn more about it in the docs: https://pris.ly/d/prisma-schema
 datasource db {
   provider = "postgres"
-  url = "***"
+  url = "***"
 }
 generator client {
   provider = "prisma-client-js"
@@ -14,8 +14,19 @@
 model Building {
   building_id             Int       @default(autoincrement()) @id
   construction_year       Int
+  construction_date       DateTime
+  use_category            String
+  heating_category        String
+  fuel_category           String
+  construction_material   String
+  area_living             Float
+  area_floors             Float
+  floors                  Int
+  location_street_address String
+  location_street_number  Int
+  location_post_number    String
 }
 model Renovation {
   id                      Int       @default(autoincrement()) @id
```


